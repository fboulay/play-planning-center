What is it ?
============

Sample application made with Play! framework

Requirements
------------

This project needs Java.
The [Play! framework](http://www.playframework.org/) must be installed too. The minimum supported version is 1.2.

Launch the project
------------------

1. Fork the project
2. Run `play dependencies` to get all libraries
3. Run `play run` to launch the app
4. Go to http://localhost:9000 to test it !
